# Pinned versions match the local research environment.
# Keep in sync with the deps installed in
# `.github/workflows/sweep.yml` step "Install dependencies".

xgboost==2.1.3
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.6.1
pyarrow==17.0.0
python-dotenv==1.0.1

# yfinance + http helpers used by feature modules (closeable_gaps, etc.)
yfinance==0.2.50
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0

# Technical-indicator library used by build_features() / local-parquet path.
# On GH Actions with BACKTEST_DATA_SOURCE=yfinance_daily a pure-pandas fallback
# is used instead, but installing it is harmless and keeps local runs compatible.
pandas-ta-classic==0.4.47

# VADER sentiment used by news_sentiment_features.py (optional — fails gracefully).
vaderSentiment==3.3.2

# Time/holidays utilities
python-dateutil==2.9.0.post0
pytz==2024.2
